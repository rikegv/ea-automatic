# EA AUTOMATIC — variáveis da infra local. Copie para infra/.env e ajuste.
# infra/.env é git-ignored (segredos via env, fail-fast no compose).

# Postgres
POSTGRES_USER=ea
POSTGRES_PASSWORD=troque-este-segredo
POSTGRES_DB=ea_automatic
POSTGRES_PORT=5433

# Redis
REDIS_PORT=6380
